#include "binary_trees.h"

/**
 * heap_to_sorted_array - heap to sorted arrays
 * @heap: heap to convert
 * @size: predetermind size of array
 * Return: pointer to the array
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	(void)heap;
	(void)size;
	return (NULL);
}
