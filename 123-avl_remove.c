#include "binary_trees.h"

/**
 * avl_remove - removes from an avl tree
 * @root: root of tree
 * @value: value to remove
 * Return: deleted node
 */
avl_t *avl_remove(avl_t *root, int value)
{
	(void)root;
	(void)value;

	return (NULL);
}
