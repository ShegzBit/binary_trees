#include "binary_trees.h"

/**
 * array_to_avl - builds an avl from an array
 * @array: pointer to start of array
 * @size: size of array
 * Return: pointer to new root
 */
avl_t *array_to_avl(int *array, size_t size)
{
	if (!array)
		return (NULL);
	(void)size;

	return (NULL);
}
