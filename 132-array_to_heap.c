#include "binary_trees.h"

/**
 * array_to_heap - array to heap
 * @array: array to convert
 * @size: size of array
 * Return: heap root pointer
 */
heap_t *array_to_heap(int *array, size_t size)
{
	(void)array;
	(void)size;
	return (NULL);
}
