#include "binary_trees.h"

/**
 * binary_tree_is_heap - betty(ed) template for the task
 * finds if a binary tree is a binary heap
 * @tree: tree to work on
 * Return: it's meant to return 0 on false and 1 on true
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
	(void)tree;
	return (0);
}
