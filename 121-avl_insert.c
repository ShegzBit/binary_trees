#include "binary_trees.h"

/**
 * avl_insert - inserts into an avl tree
 * @tree: tree root
 * @value: value to insert
 * Return: pointer tp new node
 */
avl_t *avl_insert(avl_t **tree, int value)
{
	(void)tree;
	(void)value;
	return (NULL);
}
