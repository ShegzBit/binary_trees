#include "binary_trees.h"

/**
 * sorted_array_to_avl - array to avl
 * @array: array
 * @size: size of array
 * Return: poitner to heap
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	(void)array;
	(void)size;
	return (NULL);
}
